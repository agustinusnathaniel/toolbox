export const version = "1.6.1";
export const date = "2026-06-05";
export const tag = "improved";
export const title = "Better Toast Placement";

PWA update toasts now sit at the bottom on mobile for easier thumb reach, and move to the top-right on desktop.

### Improvements

- **Responsive toast position** — update notifications appear at the bottom-center on mobile and top-right on desktop
- **Shorter tool URLs** — QR Code, JS Perf, and EV Charging tools now have cleaner paths (`/qrcode`, `/js-perf`, `/ev-charging`). Old URLs redirect automatically.
