export const version = "1.6.6";
export const date = "2026-07-07";
export const tag = "polished";
export const title = "Tool Fixes & Performance Improvements";

### Fixes & Improvements

- **QR Code Generator** — vCard addresses with optional fields left blank now produce correctly formatted output
- **JS Perf Comparator** — shows a clear error message when the benchmarking engine fails to initialize, instead of silently timing out
- **EV Charging Estimator** — calibration consistently scales the "charging above 80%" comparison metric
- **Zippy Image** — uses less memory when loading large images for dimension detection
